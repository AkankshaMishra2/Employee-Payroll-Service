# Server configuration
server.port=8082
spring.application.name=payroll-service

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Employee_Payroll_System
spring.datasource.username=root
spring.datasource.password=Anushi@4849
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.max-age=1800
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.persistent=true

# Role-based Security Configuration
# HR User: Can manage payroll operations
# ADMIN User: Can manage employees (from employee-service)
# Demo Credentials:
# Username: hr, Password: hr123, Role: HR (can access payroll + cron jobs)
# Username: admin, Password: admin123, Role: ADMIN (employee management only)

employee.service.url=http://localhost:8081/api/employees

# Automated Payroll Processing Configuration
# Cron job runs monthly on 1st day at 2:00 AM
# Daily attendance sample data generation at 1:30 AM (for testing)