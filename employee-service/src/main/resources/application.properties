# Server configuration
server.port=8081
spring.application.name=employee-service

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Employee_Payroll_System
spring.datasource.username=root
spring.datasource.password=Anushi@4849
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.max-age=1800
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.persistent=true

# Role-based Security Configuration
# ADMIN User: Can manage employees
# HR User: Can manage payroll
# Demo Credentials:
# Username: admin, Password: admin123, Role: ADMIN
# Username: hr, Password: hr123, Role: HR

payroll.service.url=http://localhost:8082